services:
  - type: web
    name: kisansetu
    env: python
    buildCommand: "pip install -r requirements.txt && python data/seed.py && python ml/train.py"
    startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
